@model Storm.Business.ViewModels.HeroTypeViewModel

@{
    ViewBag.Title = "Edit";
}

<h2>Create</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(model => model.HeroTypeId)
    <div class="form-horizontal">
        <hr />
        @Html.Partial("_ErrorSummary")

        <div class="form-group">
            @Html.LabelFor(model => model.HeroTypeName, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.HeroTypeName, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.HeroTypeName, "", new { @class = "text-danger" })
            </div>
        </div>
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                @Html.ActionLink("Back to List", "Index") | <input type="submit" value="Edit" class="btn btn-success" />
            </div>
        </div>
    </div>
}
